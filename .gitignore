# Rust build（pkg/ 是 wasm-pack 輸出 → 要 commit，CF Pages 不編 Rust）
wasm/target/
tools/sim-diff/target/
tools/sim-diff/golden.json

# Python
__pycache__/
*.pyc

# OS
Thumbs.db
.DS_Store
.adversarial-reviews/
_site/
.wrangler/
# 委派任務書（delegate-coding）等一次性工作檔（.gitignore 不支援行尾註解，勿併行寫）
tmp/
# deploy-prepare.sh 的暫存清單：不進 gitignore 會撞頂層分類閘（健檢 R5 M5）
.deploy-filelist.tmp

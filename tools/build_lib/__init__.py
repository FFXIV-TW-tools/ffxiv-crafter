# -*- coding: utf-8 -*-
"""build_lib — build-data.py 的實作層（主檔只留 CLI 旗標編排與 PROBLEMS 收尾）。

拆自原 509 行的 tools/build-data.py（2026-09-06，健檢 R5 B-034 ③）：每支模組＝一個產出職責，
檔頭註解就是該段的「為什麼」。**輸出必須逐 byte 相同**——這一層是純搬移，不改任何邏輯。
"""

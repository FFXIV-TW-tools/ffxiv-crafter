# ffxiv-crafter 修復計畫（2026-09-05・R5）

> 依據：同目錄 [`2026-09-05-R5全維健檢-health-review.md`](2026-09-05-R5全維健檢-health-review.md)
> 快照：commit `a05be8a`，working tree clean。
> 機械基線（只准升不准降）：`test-formulas` **654**／`check-actions` 35／`cargo test` 5／`run-all` 4 個測試檔。
> ⚠️ **本輪 fan-out 涵蓋率 5/12**（session 限額）——批次 R 是「把健檢做完」，不是修復。

---

## 批次 0 — 不需拍板，本輪直接執行

> 判準：Owner 明示「不需我拍板的直接完成」＝ confirmed、且**不改對外契約**、**不擴大 scope** 的項目。
> 每一項都先讓對應的哨兵／測試會紅，再修，再做突變驗證。

### 0-1 `first-run-hint-key` 哨兵的 `\b` 被寫成字面 backspace（M1・本輪頭條）

- **動機**：`tests/first-run-hint-key.test.mjs:47` 的正則實際是 `/<0x08>(defer|async)<0x08>/`，
  永遠不可能匹配 ⇒ 「不得 defer／async」那半條是死的。實測 `defer` 綠、`module` 才紅。
- **檔案**：`tests/first-run-hint-key.test.mjs:47`（2 個位元組）
- **做法**：把兩個 `0x08` 換成正確的 `\b`（成為 `/\b(defer|async)\b/`）。
- **驗證（已在副本預跑）**：乾淨 → 綠；`defer` → 紅；`async` → 紅；`module` → 紅（原本就會紅）。
- **順帶**：全 repo 掃過，文字檔只有這一處中招（`git ls-files | xargs grep -lP '\x08'` 其餘皆二進位）。
- **依賴**：無

### 0-2 改食藥後「最低能力要求」不刷新（M2・使用者側最痛）

- **動機**：`app.js:206 onConsumableChange()` 只做 `updateEff` / `updateConsumableSummary` /
  `invalidateResults`，**不呼叫 `refreshSelectedGear()`**；而紅字 `.ri-stat--req` 與 `solve-btn` 的
  `aria-disabled` 只在 `refreshSelectedGear` 內更新。`statShortfall` 的比較基準含食藥
  ⇒ 吃了藥已達標，畫面仍寫「你還差 作業 380」、求解鈕仍讀成不可用。
- **檔案**：`app.js:206`
- **做法**：`onConsumableChange()` 補 `if (selected) refreshGearNote();`，放在 `invalidateResults()` 之前
  （與 `CraftGear.afterInput`／`CraftSync.onChange` 走同一條路，不新增第二種刷新機制）。
- **驗證**：新增測試——有 `required_craftsmanship` 的配方 ＋ 剛好不足的 gear ＋ 提供加成的
  `CraftConsumable` stub → 呼叫 `onConsumableChange` → 斷言 `solve-btn` 的 `aria-disabled === 'false'`
  且 `.ri-stat--req` 不再帶 `is-short`。**突變驗證**：拿掉那一行必須轉紅。
- **依賴**：無

### 0-3 食藥資料載入失敗會清空玩家偏好（M3）

- **動機**：`app.js:86-87` 的 `fetchOpt` 失敗回 `[]`；`app-consumable.js:179 setData` 無條件把
  「不在 MAP 裡的保存值」當幽靈選擇清掉 ⇒ **一次網路抖動＝偏好被清**，且下一次任何 `save()`
  （HQ 勾、`<details>` 開合）就寫回 localStorage 成為永久。
- **檔案**：`app.js:86-87`／`app-consumable.js:179-185`
- **做法**：把「載入失敗」與「品項下架」分開——`app.js` 在 meals/medicine **任一失敗時不呼叫
  `setData`**（最小改動、不新增哨兵值）；`setData` 維持原語意。
- **⚠️ 連帶必修**：`T15` 現行用**空陣列**當「資料改版」的樣本 ⇒ 它把這個 bug 當規格釘住了。
  樣本改成「非空但缺該品項的清單」，另加一條「`setData([], [])` 後保存值不得被清」。
- **驗證**：改完 T15 應先紅再綠；突變驗證＝把 `setData` 改回無條件呼叫必須轉紅。
- **依賴**：無

### 0-4 `THIRD-PARTY-NOTICES.md` 沒有隨站發佈（M4・授權義務）

- **動機**：線上實查 `/THIRD-PARTY-NOTICES.md` → **404**，而被服務的 `LICENSE-MIT.txt:4` 明寫
  「各套件的著作權人清單見 THIRD-PARTY-NOTICES.md」。站上散布 `pkg/crafter_wasm_bg.wasm`
  含多個純 MIT 套件，MIT 要求著作權宣告隨副本散布；AGENTS.md 鐵則 10 自己就寫「頁尾只寫授權名稱不算」。
- **檔案**：`deploy-deny.txt:12`／`deploy-allow.txt`／`deploy-prepare.sh`（step 3 的 `*.md` 二次清理）
- **做法**：**不能只把它從 deny 搬到 allow**——`deploy-prepare.sh` step 3 會無條件刪掉輸出裡的
  `*.md`（現行只對 `LICENSE*.txt` 開例外）。兩個做法擇一：
  - **(a)（建議）** `build-notices.py` 改產 `THIRD-PARTY-NOTICES.txt`（純文字，內容不變），
    列入 `deploy-allow.txt`，`LICENSE-MIT.txt:4` 與 `index.html` 頁尾改指向它。`.md` 版留在 repo 供閱讀。
  - (b) 在 step 3 的 `find ... ! -name 'LICENSE*.txt'` 加一個 `! -name 'THIRD-PARTY-NOTICES.md'` 例外。
    改動最小，但在「一律不發 `.md`」的規則上開了一個逐案例外。
- **驗證**：`sh deploy-prepare.sh` → 輸出含該檔；部署後帶 cache-bust `curl -sI` 回 200。
  加一條 `tests/` 斷言：`build-notices.py` 的產物檔名同時出現在 `deploy-allow.txt`
  且不匹配 step 3 的刪除樣式。
- **依賴**：無。**注意**：真正生效需 push（部署屬 STOP，Owner 觸發）。

### 0-5 `.deploy-filelist.tmp` 自鎖（M5）

- **動機**：該檔名同時缺席於 `.gitignore`／`deploy-allow.txt`／`deploy-deny.txt`／
  `deploy-prepare.sh:46` 分類閘的 `case` 略過清單（只有 `:80` 的複製迴圈裡跳過它）。
  實測 `touch .deploy-filelist.tmp` 後跑腳本 → 「✗ 頂層出現未分類項目：.deploy-filelist.tmp」，
  **而訊息建議「站台資產請加進 deploy-allow.txt」——照做等於把暫存檔加進部署面**。
- **檔案**：`.gitignore`
- **做法**：`.gitignore` 加一行 `.deploy-filelist.tmp`。**刻意不改腳本**——腳本第 1 段已經有
  `git check-ignore -q "$e" && continue`，走既有機制即可，不在 12 repo 共用的腳本上開分岔。
- **驗證**：`touch .deploy-filelist.tmp && sh deploy-prepare.sh` 應正常輸出 44 檔；刪回後再跑一次仍綠。
- **依賴**：無

### 0-6 測試基線的第二事實源（M6）

- **動機**：`AGENTS.md:86` 散文寫 `test-formulas` **653**，而 `TEST-BASELINE` 標記與實測都是 **654**。
  gate 6 只解析標記 ⇒ 散文那份靜默腐爛。另外 **`run-all` 沒有標記**：刪掉 `tests/` 任一支測試檔後
  `run-all` 會印「3/3 測試檔通過」並 exit 0，canonicalTest 與 pre-commit 都不會紅。
- **檔案**：`AGENTS.md:81-86`
- **做法**：① 把 :86 的四個數字全部拿掉，改成「宣告值見上方 `TEST-BASELINE` 標記」；
  ② 補一條標記：
  `<!-- TEST-BASELINE cmd="node tests/run-all.mjs" match="(\d+)/\d+ 測試檔通過" expect="4" label="run-all" -->`
- **驗證**：`node ../../tools/check-test-baseline.js --repo .` 應變成檢查 4 項且 0 不符。
- **依賴**：無

### 0-7 `opt-adversarial` 的保存偏好被 expert 配方吃掉（M7）

- **動機**：與前輪 P0-5／T43 是**同一根因、同一次改動沒改乾淨**——`specWanted` 只涵蓋
  `SPEC_GATED_IDS` 兩個 id，第三個被程式強制取消的 `opt-adversarial` 不在內。
  序列：勾好防球 → 選 expert 配方（強制取消＋disabled）→ 改任一其他選項觸發 `saveSolveOpts`
  → localStorage 被寫成 `opt-adversarial:false`，之後所有一般配方都預設不防球。
- **檔案**：`app.js:315-336`（`SOLVE_OPT_IDS`／`specWanted`／`saveSolveOpts`）＋ `app-recipe.js:284-286`
- **做法**：把「被程式強制取消的偏好」一般化——`saveSolveOpts` 對**任何 disabled 的 id** 寫回記錄值
  而不是 DOM 值（不再逐 id 列舉，避免第四個 id 出現時再漏一次）。
- **驗證**：擴充 T43——選 `is_expert` 配方讓 `opt-adversarial` 被強制取消後呼叫 `saveSolveOpts`，
  斷言 localStorage 內該值仍是玩家原本的 `true`。突變驗證：改回逐 id 列舉必須轉紅。
- **依賴**：無

### 0-8 `AbortSignal.timeout` 無 feature detect（M8）

- **動機**：它是整條資料載入路徑上唯一 2022 才普及的 API，且失敗發生在 `fetch` **之前**，
  連 `fetchOpt` 的降級都吃不到 ⇒ 舊 Safari／舊 WebView 看到的是永久壞掉的站，
  而畫面給的「請重新整理」永遠無效。本輪剛做完行動適配，舊 iPad 是真實族群。
- **檔案**：`app.js:62`
- **做法**：`const sig = AbortSignal.timeout ? AbortSignal.timeout(30000) : undefined;`
  拿不到逾時就退回無逾時（比整站死掉好）。
- **驗證**：grep 型斷言——`app.js` 出現 `AbortSignal.timeout` 時必須同時出現對它的存在性判斷（禁裸呼叫）。
- **依賴**：無

**批次 0 預期 commit 數**：4（`test:` 哨兵修正＋新測試／`fix:` 前端三項行為缺陷／
`build:` 部署面兩項／`docs:` 基線第二事實源）
**驗收命令**：`node --check *.js && node tools/test-formulas.mjs && node tests/run-all.mjs && py -3.11 tools/check-actions.py && sh deploy-prepare.sh`

---

## 待拍板（不在本輪執行 — 需要 Owner 的決定，不是需要更多分析）

### ① M9 — `craftPlan` 死碼＋鑽石依賴重複計數

實測：`craftPlan` 在生產路徑上零呼叫點（UI 走的是 `craftIngredient`／返回堆疊與 `crafting-list`
的單層 `aggregateMats`），但 AGENTS.md 架構表把它列為「製作鏈」的實作、`T51` 有 8 條 golden 斷言
⇒ 一份**看起來被驗證過的假護欄**。而 T51 的三層案例是**鏈狀不是鑽石狀**，剛好避開缺陷
（成品同時直接與間接用到同一個中間材時，底層素材會重複計數；實測 W 應為 4 卻算成 6）。
同層的 `app-quests.js:54-72 expandMats` 面對同一情境是**對的**（往下傳 need 不傳 total）。

| 選項 | 做法 | 成本 | 風險 |
|---|---|---|---|
| **A（建議）** | 刪掉 `craftPlan` 與 T51 對應斷言，AGENTS.md 架構表同步改口 | 小 | 若日後真要做「跨目標合併製作計畫」需重寫（但重寫一個正確的比修一個沒人用的划算） |
| B | 保留並修正遞迴（改成先完整彙總 need、再單獨一輪展開），補一條鑽石依賴 golden | 中 | 修一段沒有消費端的程式碼；正確性只能靠 golden 保證 |
| C | 維持現狀 | 0 | **這正是 finding 本身**：文件說有、測試說綠、真接上 UI 會給出偏高的採購量而畫面完全正常 |

**建議 A**：對齊本 repo 既有處置（2026-08-15 刪三支死 proxy）與「每一行無法證明其價值的程式碼都應被刪除」。

### ② M10 — `AGENTS.md` 已長回 36,911 B，R7 豁免的立論已被證偽

豁免文字寫「已照 R7 規定做完前兩步…**36.1KB → 28.3KB**。剩下的每一條都是可執行規則，再砍就是
R7 自己警告的『為壓 byte 刪有效紅線』」，到期日 2026-11-16。
實測：豁免落地那個 commit（`0ca22de`，2026-08-17）是 **31,248 B**，現在 **36,911 B**（+18%），
**已超過當初觸發瘦身的 36.1KB**。成長集中在三段：**資料與求解 +2,743**／**UI 設計系統 +1,587**／
**VERIFY +1,333**。

⚠️ **這一條的真正形狀比「沒有哨兵」更難處理**：本輪實跑 `node ../../tools/check-devloop-artifacts.js --repo .`
當場印出 `⚠ R7 常駐層 AGENTS.md 36.0KB > 20KB`，然後仍判「✓ 工件格式合格」。
也就是說**訊號從豁免那天起就一直在每次 commit 印**，而該哨兵**並不消費 `R7-exempt` 標記**
（grep 全檔無 `exempt`）⇒ 它對「已豁免」與「未豁免」印的是同一行。
結果是這行警告被合理地當成「已知已豁免」而略過，檔案就一路長了 18%。
**修法必須讓「豁免」與「超出豁免當時的值」在機械上可分辨**，否則加再多警告都會被同樣地略過。

| 選項 | 做法 | 成本 |
|---|---|---|
| **A（建議）** | 把三段新增內容裡「已固化成測試的條目」壓成「規則一行＋測試編號」，敘事移進 `docs/lessons.md`；目標回到 ~30KB | 中（要逐條判斷哪些有測試守） |
| B | 提前撤銷豁免、照 R7 正常流程重做一次瘦身 | 中大 |
| C | 加一支哨兵把「AGENTS.md 位元組數不得超過豁免當時的值」機械化，內容暫不動 | 小（一條斷言）——**這一項無論選 A/B 都建議一起做**，否則下次還是靠人記得 |

**建議 A ＋ C**：C 是防再犯的結構性修法（豁免的失效目前零回饋訊號）；A 是把這一次的超標還回去。

### ③ M11 — 三個檔跨過 500 行 review 閘門，無拆分紀錄

前輪快照 `43b7def`：`app.js` **453**／`styles.css` **606**。現在：`app.js` **547**／`styles.css` **787**／
`tools/build-data.py` **509**。鐵則 6 要求「既有檔 >500 被實質修改時觸發拆分 review 閘門：
一句話答不出該檔職責 → 該次任務內提出拆分方案」，而三者在本區間各被改 **6／15／3** 次，
BACKLOG 與 CHANGELOG 都找不到該閘門被行使的紀錄。

**拆分候選（列給 Owner 拍板，不當場動工）**：

| 檔 | 現行職責（一句話說不清的部分） | 候選切法 |
|---|---|---|
| `app.js` 547 | 「資料載入 **和** 公式 **和** 食藥加成 **和** 分頁 **和** init 接線」 | 抽 `app-data.js`（`loadData`／11 支 JSON 的必要選配分類／索引建立）＋ `app-formula.js`（`computeSettings`／`recipeMaxes`／`effectiveStats`／`applyConsumables`／`statShortfall`）→ `app.js` 只剩 router／init／狀態持有 |
| `styles.css` 787 | 單檔涵蓋全站所有頁面 | 按既有頁面邊界切：`10-base` / `20-browse` / `30-recipe` / `40-result` / `50-quests`（對齊 marketboard 的 `styles/NN-*.css` 序載慣例，同生態一致） |
| `tools/build-data.py` 509 | 「六個資料源各自的擷取 **和** 合併 **和** 出貨」 | 抽 `build_lib/`（每個資料源一個 module）＋主檔只留編排與 `PROBLEMS` 收尾 |

⚠️ **三者都不是本輪該做的**——鐵則明說「列候選給 shawn 拍板，不當場擴大 scope」。

### ④ M12 — 301 改動把首頁綁進 Pages Functions 日額度（跨 13 站）

2026-09-02 為 SEO 把 `/` 與 `/index.html` 列進 `_routes.json` 的 include，代價是**每次開頁一次
帳號級計費 invocation**；免費硬牆 100k/日是 **13 站共用**，而 2026-08-28 才剛因此全站 Functions 停擺過。
本 repo 這側**首頁沒有任何降級路徑**（settings-api 有 503/502，首頁沒有）。
`index.html:17-20` 的成本論證註解仍寫「新網域上的訪客一次 Function 都不用付」——已與事實矛盾。

| 選項 | 做法 | 成本 | 風險 |
|---|---|---|---|
| **A（建議）** | 先驗 CF Pages 原生 `_redirects` 是否支援 host 級來源；若可，把舊 host 的 301 移出 Functions，`/` 退回純靜態 | 中（要實驗＋13 站同步） | 若 `_redirects` 不支援 host 條件則走 B |
| B | 維持 Functions，但補：① 改寫 `index.html` 的成本註解 ② `check-cf-usage.py` 的門檻把「首頁流量」計入 ③ AGENTS/BACKLOG 記一筆「首頁已在額度後面、無降級路徑」 | 小 | 沒有消除風險，只是讓它可見 |
| C | 撤回 301 改回 canonical | 小 | **不可行**：GSC 實查已證明 Google 否決 canonical、把 pages.dev 選成標準網址 |

**建議 A，退而求其次 B**。⚠️ **這是 13 站共用形狀，不該單邊改**——本 repo 只做 B 的第 ①項（改註解）
屬批次 0 範圍外的文件修正，已列入建議段。

### ⑤ M13 — 行數鐵則的機械閘看不到 `.mjs`

repo 最大的手寫檔 `tools/test-formulas.mjs` **3129 行**，遠超 2000 紅線，本輪被改 13 次零警告——
因為 monorepo `tools/git-hooks/pre-commit` gate 3 的副檔名清單只有 `.js` 沒有 `.mjs`。

| 選項 | 做法 | 影響面 |
|---|---|---|
| **A（建議）** | gate 3 的副檔名清單補 `.mjs`，並同時決定 `test-formulas.mjs` 是否列入「純測試檔」白名單 | 跨 repo（monorepo hook，13 個 external repo 共用）——**要 Owner 拍板才動** |
| B | 只在本 repo 加一條 repo-local 斷言 | 小，但把「該由共用閘做的事」下放到單一 repo |
| C | 維持現狀 | 紅線對 `.mjs` 永遠不生效 |

---

## 批次 R — 補跑未覆蓋的 7 個維度（**不是修復，是把健檢做完**）

session 限額於 **2pm（Asia/Taipei）** 重置後，直接續跑（已完成的 13 個 agent 吃快取、零重複成本）：

```
Workflow({ scriptPath: 'tmp/review-workflow.js', resumeFromRunId: 'wf_600e4efb-4b7' })
```

未覆蓋：`correctness-data`／`docs-drift`／`tests-ci`／`build-release`／`design-system`／`perf-ux`／`ux-flows`
＋ recall 軌 B 的 7 個 spreadKey ＋ 軌 C 的驗證階段。
⚠️ **批次 0 若先執行，`tmp/review-workflow.js` 的 CONTEXT 就與工作樹不同步**——
續跑前要嘛先跑批次 R、要嘛接受 reviewer 看到的是修好之後的碼（那會讓 M1–M8 不再被獨立確認，
但它們已有主迴圈的機械證據與突變驗證）。**建議順序：批次 R → 批次 0。**

---

## 執行備註

- **commit 顆粒度按主題切**（對齊 monorepo `.claude/rules/git-workflow.md`）；健檢工件（報告／計畫／`_INDEX`）
  自成一筆，與批次 0 的修復**分開 commit**。
- **push 屬 STOP**：`bash ~/.claude/skills/process/tools/safe-push.sh --repo C:/FFXIVProject/external/ffxiv-crafter --reason "<原因>"`，由 Owner 觸發。
  0-4 的授權檔要 push 才真的生效。
- **測試基線只准升不准降**：批次 0 完成後更新 `AGENTS.md` 的 `TEST-BASELINE` 標記（不是散文，見 0-6），
  敘事寫進 `docs/test-baseline-history.md`。
- **每個新增／修改的哨兵都要做突變驗證**（注入原本那個缺陷 → 必須精準轉紅 → 還原即綠）。
  M1 的突變驗證已在副本上預跑完成（乾淨綠／defer 紅／async 紅／module 紅）。
- **0-2／0-3／0-7 動到前端行為**，`node tools/test-formulas.mjs` 綠**不構成驗收**：
  另跑 `py -3.11 tools/serve.py`（:8809）＋ portal svc :8774 的瀏覽器 smoke，
  重現路徑＝①選有最低能力要求的配方 → 看到紅字 → 選食物 → 紅字要消失且求解鈕轉可用
  ②勾防球 → 選 expert 配方 → 改別的選項 → 回一般配方，防球要還在
  ③斷網重整 → 食藥選擇要還在。
- **本計畫尚未過獨立計畫審 gate**（session 限額）。批次 0 的八項都不動資料模型／儲存／對外 API
  （0-4 只是把一個既有檔加進發佈清單），依 gate 觸發條件屬「小型」可略過；
  **但待拍板的 ①（刪碼）與 ④（跨 13 站路由）在動工前應各自過一次 gate。**

# ffxiv-crafter 修復計畫（2026-09-05・R5，補齊 12 維後的最終版）

> 依據：同目錄 [`2026-09-05-R5全維健檢-health-review.md`](2026-09-05-R5全維健檢-health-review.md)
> 快照：commit `a05be8a`。機械基線（只准升不准降）：`test-formulas` **654**／`check-actions` 35／`cargo test` 5／`run-all` 4。

---

## 批次 0 — 不需拍板，本輪直接執行（16 項）

> 判準：confirmed、不改對外契約、不擴大 scope、不動 13 站共用樣板（`_middleware.js`／部署面鐵則段）、
> 不動 monorepo hook。每一項先讓哨兵會紅、再修、再突變驗證。

| # | 項目 | 檔案 | 做法 | 守 |
|---|---|---|---|---|
| 0-1 | M1 哨兵 `\b` 是字面 0x08 | `tests/first-run-hint-key.test.mjs:47` | 兩個 0x08 → `\b`（2 位元組）。副本已驗：乾淨綠／defer 紅／async 紅／module 紅 | 自守 |
| 0-2 | M2 改食藥後門檻不刷新 | `app.js:206` | `onConsumableChange` 補 `if (selected) refreshGearNote();`（走 `CraftGear.afterInput` 同一條路） | 新 T62：食藥 stub 跨門檻後 `solve-btn[aria-disabled]==='false'` 且 `.ri-stat--req` 無 `is-short` |
| 0-3 | M3 食藥載入失敗清偏好 | `app.js:94` | meals／medicine 任一失敗（`[]`）時**不呼叫 `setData`**；T15 的「品項下架」樣本改成非空清單 | T15 改寫＋新斷言「`[]` 不清保存值」 |
| 0-4 | M4 授權檔未發佈＋README 前提翻掉 | `tools/build-notices.py`／`deploy-allow.txt`／`deploy-deny.txt`／`LICENSE-MIT.txt:4`／`README.md:46`／`index.html` 頁尾 | 產物改名 **`LICENSE-THIRD-PARTY.txt`**（同時命中 `deploy-prepare.sh:105`／`:137` 既有 `LICENSE*.txt` 例外 ⇒ **共用腳本零改動**）；allow 加、deny 移除；三處指標改指它；README 改成事實陳述 | 新斷言：`index.html` 頁尾與 `LICENSE-MIT.txt` 指向的授權檔都在 `deploy-allow.txt` |
| 0-5 | M5 `.deploy-filelist.tmp` 自鎖 | `.gitignore` | 加一行（腳本第 1 段已有 `git check-ignore` 分支，走既有機制、不改共用腳本） | 手動：touch 後 `sh deploy-prepare.sh` 仍 44 檔 |
| 0-6 | M6 基線第二事實源＋run-all 無下限 | `AGENTS.md:81-86`／`tests/run-all.mjs` | :86 數字全拿掉改指標記；補 `TEST-BASELINE cmd="node tests/run-all.mjs" … expect="4"`；run-all 加 `files.length < 4 ⇒ exit 1` | gate 6 變 4 項 |
| 0-7 | M7 `opt-adversarial` 偏好被吃 | `app.js:315-340,490`／`app-recipe.js:284-286` | `specWanted` 一般化為 `optWanted`（全部 `SOLVE_OPT_IDS`）；save 對任何 disabled id 寫記錄值；`app-recipe` 離開 expert 時 `deps.restoreOpt('opt-adversarial')` | T43 擴充：expert 來回後 localStorage 與 DOM 都保留 true |
| 0-8 | M8 `AbortSignal.timeout` 無退場 | `app.js:62` | `AbortSignal.timeout ? … : undefined` | 新 grep 斷言：出現 `AbortSignal.timeout` 必伴存在性判斷 |
| 0-9 | M14 `isCrystal` 誤判 55 筆 | `app.js:346` | 判準改 `iid < 20 \|\| category === '水晶'`（`items.json` 每筆有 `category`）；名稱正則移除。deps 已注入 `getItems` | 新斷言：對 `ingredients.json` 全部素材，`isCrystal===true ⇒ category==='水晶'`（現況抓出 55） |
| 0-10 | M16 BUILD-STAMP 不雜湊產物＋裸 wasm-pack 零防護 | `tools/build-wasm.ps1`／`tools/check-actions.py`／`wasm/BUILD-STAMP.json` | stamp 加 `pkg_wasm`／`pkg_js` sha256（build 寫、check 驗，任一單邊變動即紅）；check-actions 直接掃 `pkg/*.wasm` bytes 無 `Users\`／`.cargo\registry`；stamp 以現 pkg 補齊欄位（不重建） | 自守；突變：改 pkg 一位元組必紅 |
| 0-11 | M19 ratchet 門檻低於宣告 | `tools/test-formulas.mjs:1896,2187` | T20 `>=700`→`>=768`、T32 對齊 247；抽成同一常數 | 自守 |
| 0-12 | M18 首次提示死鈕 | `app.js:463,513` | `goto-stats-hint` 綁定與 `renderGearsets()` 移到 `await loadData()` 之前 | T42 補：停在 await 時 `onclick` 為函式、`#gearsets` 已含 `gear-in` |
| 0-13 | M20 T36 手維護清單漏 `.consumables`＋`.rt-patch` 無 label | `tools/test-formulas.mjs:2337`／`app-browse.js:145`／`styles.css` | T36 清單改由 markup `codex-tint-panel--neutral ([a-z-]+)` 反推＋涵蓋率閘（<5 即紅）；`.rt-patch` 加 `data-label="版本"`；T11 對帳改「數字欄數 == data-label 數」 | 自守 |
| 0-14 | M21 漏 preconnect | `index.html:82` | 加 `<link rel="preconnect" href="https://v2.xivapi.com">`（**不加 `crossorigin`**） | 新斷言：`iconUrl()` host 必有 preconnect |
| 0-15 | M23 文件漂移三處 | `AGENTS.md:20,91`／架構表／`docs/lessons.md:89` | `hqPercent`→`app-render.js`；補 `first-run-hint.js`／`404.html`／`tests/` 三列；「T1〜T57」→T61；lessons 標題改 | 目視 |
| 0-16 | correctness-data A3 `jobCount` 把配方數當職業數 | `app-nextcraft.js:59` | `new Set(list.map(e=>e.recipe.job)).size` | T57 補：同職兩配方 ⇒ jobCount 1 |

**刻意不進批次 0**（理由）：docs A4 部署後驗 wording 與 resilience A4 交接腳本空 catch（13 站共用段落／樣板，不單邊改）；
build A4 LIST `mktemp` 根治（動共用腳本，0-5 已消症狀）；ux A2-A4（partial low，設計取捨）；design A1/A2（視覺改動需目視驗收）。

**驗收命令**：`node --check *.js && node tools/test-formulas.mjs && node tests/run-all.mjs && py -3.11 tools/check-actions.py && sh deploy-prepare.sh && node ../../tools/check-test-baseline.js --repo .`
**瀏覽器 smoke**（0-2／0-3／0-7／0-9／0-12 動前端行為，`test-formulas` 綠不構成驗收）：`py -3.11 tools/serve.py` :8809 ＋ portal :8774 →
①選有最低能力要求的配方看到紅字→選食物→紅字消失、求解鈕可用 ②勾防球→選 expert→改別的選項→回一般配方防球仍在
③載入期按「前往角色數值 →」要能切頁且面板有內容 ④製造清單裡「紫水晶手鐲」類素材要在可自製組且有「加進清單」。

---

## 待拍板（需要 Owner 的決定，不是需要更多分析）

### ① B-032 — `craftPlan` 死碼＋鑽石依賴重複計數（M9）
| 選項 | 做法 | 成本／風險 |
|---|---|---|
| **A（建議）** | 刪 `craftPlan` 與 T51 對應斷言，AGENTS 架構表改口，加「匯出的純函式必有呼叫端」掃描斷言 | 小；對齊 2026-08-15 刪死 proxy 的處置 |
| B | 保留並改成兩階段（先彙總 need 再展開）＋鑽石 golden | 中；修一段沒消費端的碼 |
| C | 維持現狀 | 0；＝finding 本身 |

### ② B-033 — AGENTS.md 長回 36.9KB、R7 警告每次都在印（M10）
成長：資料與求解 +2,743／UI 設計系統 +1,587／VERIFY +1,333。**gate 5 每次印 `⚠ R7 … 36.0KB > 20KB` 後仍判合格，且不消費 `R7-exempt` 標記** ⇒ 對「已豁免」與「超出豁免當時的值」印同一行，警告被合理地略過。
| 選項 | 做法 |
|---|---|
| **A（建議）** | 三段新增內容裡已固化成測試的條目壓成「規則一行＋測試編號」，敘事移 `docs/lessons.md`，目標 ~30KB |
| B | 提前撤銷豁免、照 R7 重做瘦身 |
| **C（無論 A/B 都建議一起做）** | 加一條「AGENTS.md 位元組數不得超過豁免當時的 31,248」的機械斷言，讓豁免與超標可分辨 |

### ③ B-034 — 三檔跨過 500 行閘門（M11）
| 檔 | 一句話說不清的部分 | 候選切法 |
|---|---|---|
| `app.js` 547 | 資料載入 **和** 公式 **和** 食藥 **和** 分頁 **和** init | 抽 `app-data.js`（loadData／JSON 分類／索引）＋ `app-formula.js`（computeSettings／recipeMaxes／effectiveStats／applyConsumables／statShortfall／isCrystal） |
| `styles.css` 787 | 單檔涵蓋全站 | `10-base`／`20-browse`／`30-recipe`／`40-result`／`50-quests` 序載（對齊 marketboard 慣例） |
| `tools/build-data.py` 509 | 六個資料源擷取 **和** 合併 **和** 出貨 | 抽 `build_lib/` 每源一 module，主檔留編排＋`PROBLEMS` |

### ④ B-035 — 301 把首頁綁進 13 站共用 Functions 日額度（M12）
| 選項 | 做法 |
|---|---|
| **A（建議）** | 驗 CF Pages `_redirects` 是否支援 host 級來源；可則 301 下放靜態層，`/` 退出 include |
| B | 維持 Functions，補三件：改 `index.html:17-20` 註解／`check-cf-usage.py` 計入首頁流量／AGENTS 記「首頁在額度後面、無降級」 |
| C | 撤回 301 —— **不可行**（GSC 已證明 Google 否決 canonical） |
13 站共用形狀，不單邊改。

### ⑤ B-036 — 一物多配方：同職多張時職業切換列出現同名鈕（M15）
136 組（成品,職業）有 2〜4 張配方；`pickRecipeForItem` 同職多筆時取先出現者、規則未明寫。
| 選項 | 做法 |
|---|---|
| **A（建議）** | 鈕面帶可分辨資訊（`rlv`／難度），`pickRecipeForItem` 明寫「同職取難度最低」；golden：同職兩張 rlv 不同 ⇒ 鈕文字互異 |
| B | 先按職業分組、組內二次選擇（UI 多一層） |
| C | 只修 `pickRecipeForItem` 規則、鈕不動（仍不可分辨） |
需要 Owner 決定「怎麼標」——這是 UI 語彙。

### ⑥ B-037 — 機械網的觸發與安裝面（M17＋M13＋build A6）
四件同根：① gate 6 `--staged` 觸發正則不匹配 `tools/test-formulas.mjs` ② gate 3 只看 `.js` 不看 `.mjs`（3129 行檔零警告）③ `wasm/` 改動不觸發 cargo test ④ hooksPath 在 `.git/config` 指 repo 外絕對路徑、新 clone 零閘門；另 deploy-prepare.sh 不在 canonicalTest。
| 選項 | 做法 |
|---|---|
| **A（建議）** | monorepo `check-test-baseline.js --staged` 改由各 repo `TEST-BASELINE` 標記的 `cmd`／`cwd` 反推監看路徑（宣告什麼監看什麼）；gate 3 副檔名補 `.mjs`；本 repo 加 `tests/hooks-installed.test.mjs`；fleet.json canonicalTest 尾接 `sh deploy-prepare.sh` |
| B | 只做本 repo 側（hooks 測試＋repo-local 行數斷言），hook 不動 |
| C | 維持現狀 |
**跨 repo hook ＋ fleet.json，必須拍板。**

### ⑦ B-038 — 引擎工具鏈未釘（M22）
`rust-toolchain` 裸 `nightly`、`wasm-pack` 版本無處可查。
| 選項 | 做法 |
|---|---|
| **A（建議）** | channel 釘 `nightly-YYYY-MM-DD`（現行可編那天）；`build-wasm.ps1` 把 `rustc -V`／`wasm-pack -V` 寫進 BUILD-STAMP；check-actions 斷言 channel 形狀 |
| B | 只記版本不釘 |
需要一次真實重建驗證（wasm-pack 工具鏈）才能收，故拍板。

---

## 執行結果（2026-09-05 當輪完成）

**批次 0 十六項全數完成並 commit（3 筆，未 push）**：

| commit | 內容 |
|---|---|
| `2e372cd` | **fix**：M1／M2／M3／M7／M8／M14／M18／M19／M20／M21／A3（jobCount）＋ T62・T63 新增與 T15／T36／T41／T42／T43／T56／T11／T48 擴充；AGENTS.md 基線改指標記、run-all 標記、hqPercent 指標、架構表補三列、T 編號 |
| `9e98fa1` | **build**：M4（`LICENSE-THIRD-PARTY.txt`，共用腳本零改動）／M5（`.gitignore`）／M16（戳記加 `pkg_wasm`／`pkg_js`＋產物 bytes 掃描） |
| （本筆） | **docs**：CHANGELOG 2026-09-05 段、`docs/lessons.md` 標題、本節 |

**測試基線**：`test-formulas` 654 → **683**；`run-all` 補標記 4；`check-actions` 不變量 3（第二條現在真的驗產物）。
**突變驗證**：13 組「把原缺陷放回去」逐一轉紅（另 M1 在副本上四向驗證）。
**瀏覽器 smoke**（AGENTS 明文 `test-formulas` 綠不構成驗收；本機 :8809 ＋ portal CDN :8774，Playwright）**8/8**：
①選有門檻的配方（古典風格長槍 3180，gear 3170）→ 紅字＋aria-disabled → 選新薯泥（+12）→ 紅字消失、鈕可用
②expert 配方防球被強制取消 → 改別的選項存檔 → localStorage 仍 true → 回一般配方勾回來
③把 `recipes.json` 延遲 3.5s，載入期「前往角色數值 →」已綁定、面板有輸入格、按下真的切頁
④配方 811 紫水晶手鐲：紫水晶排在真晶體之前。console 殘餘錯誤只有臨時 CDN 對 `announcements*.json` 無 CORS 與 dev-mode `localhost:8787` 未開，與 R4 記錄相同、非站台。
**收尾閘**：`node --check` 15 支／`run-all` 4/4／`deploy-prepare` 45 檔（含 `LICENSE-THIRD-PARTY.txt`）／`check-test-baseline` 4 項相符／`check-devloop-artifacts` 合格；pre-commit 另抓到我改 `.ps1` 時吃掉 BOM，已用 `check-ps1-bom.js --fix` 補回。

**未執行、待拍板（BACKLOG）**：B-032〜B-038。

## 執行備註
- 健檢工件自成 commit；批次 0 按主題切 4〜5 筆（test 哨兵／前端行為／build＋授權／docs）。
- **push 屬 STOP**：`bash ~/.claude/skills/process/tools/safe-push.sh --repo C:/FFXIVProject/external/ffxiv-crafter --reason "<原因>"`。0-4 要 push 才真的生效。
- 基線只准升不准降：批次 0 後更新 `TEST-BASELINE` 標記（不是散文），敘事寫 `docs/test-baseline-history.md`。
- 每個新增／修改哨兵做突變驗證（注入原缺陷→精準紅→還原綠）。
- **獨立計畫審 gate**：批次 0 十六項皆不動資料模型／儲存／對外 API（0-4 是把既有檔加進發佈清單、0-10 是 stamp 多兩欄）⇒ 小型可略；①（刪碼）⑥（跨 repo hook）動工前各過一次 gate。
